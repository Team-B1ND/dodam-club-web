import styled from "styled-components";

export const clubMenubar = styled.div`
  display: flex;
  flex-direction: column;
  gap: 16px;
  overflow-y: scroll;
  padding: 58px 0;
  white-space: nowrap;
`